build:
  maxIssues: 0

config:
  validation: true

console-reports:
  active: true
  exclude:
    - 'ProjectStatisticsReport'
    - 'ComplexityReport'

style:
  MagicNumber:
    active: false
  WildcardImport:
    active: false
  ReturnCount:
    max: 3
